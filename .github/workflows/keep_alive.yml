name: Keep Streamlit App Alive

on:
  schedule:
    # 8時間おきに実行 (UTC時間で 0, 8, 16時)
    - cron: '0 */8 * * *'
  workflow_dispatch: # 手動実行も可能にする

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Access Streamlit App
        run: curl -I https://rekishi-ai-app-n58vasuxhabfuhsj8xjxmm.streamlit.app/
